# ─── Base de données ────────────────────────────────────────────────────────
# URL de connexion Neon (Serverless PostgreSQL)
# Format : postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST/beatotheque?sslmode=require"

# ─── JWT ─────────────────────────────────────────────────────────────────────
# Clé secrète pour signer les tokens JWT (min. 32 caractères aléatoires)
JWT_SECRET="remplacer-par-une-cle-aleatoire-tres-longue-et-securisee"

# Durée de validité du token (ex: "7d", "24h")
JWT_EXPIRES_IN="7d"

# ─── Domaine (cookie httpOnly) ───────────────────────────────────────────────
# En développement : http://localhost:3000
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
